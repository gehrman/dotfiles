[core]
    autocrlf = false
    excludesfile = ~/.gitignore
    preloadIndex = true
    fscache = true
[color]
    ui = true
    whitespace = red
[push]
    default = current
[fetch]
    prune = true
[alias]
    co = checkout
    cwm = commit -m
    amend = commit --amend
    popstash = stash pop
    liststash = stash list
    showstash = stash show -p
[merge]
    conflictstyle = diff3
[include]
    path = .gitconfig.site
